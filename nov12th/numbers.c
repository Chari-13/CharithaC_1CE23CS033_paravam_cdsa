#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter two numbers:");

    scanf("%d %d",&a,&b);
    if(a<b)
    for(;++a<b;)
    {
        printf("%d\n",i);
    }
    else{
         printf("a should not be less than b");
    }
}